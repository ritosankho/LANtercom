users = {                  #The users dictionary. the idea in the final version is to generate a random 8 digit username and password and append it into here periodically and delete older ones.
    "admin":"admin",
    "cat":"meow"
}
